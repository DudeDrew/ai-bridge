services:
  - type: web
    name: ai-bridge
    env: python
    region: oregon
    plan: free  # Changed from starter - sleeps after 15min inactivity
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn main:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120
    
    envVars:
      - key: BRIDGE_API_KEY
        generateValue: true
      
      - key: DATABASE_URL
        fromDatabase:
          name: ai-bridge-db
          property: connectionString
      
      - key: OPENAI_API_KEY
        sync: false
      
      - key: ANTHROPIC_API_KEY
        sync: false
      
      - key: NOTION_TOKEN
        sync: false
      
      - key: OBSIDIAN_API_KEY
        sync: false
      
      - key: FLASK_ENV
        value: production

databases:
  - name: ai-bridge-db
    databaseName: ai_bridge
    user: bridge_user
    plan: free  # Changed from starter - FREE!
